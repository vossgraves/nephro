# UI validation

## Desktop landing page

The refreshed landing page loaded from the clean local Next.js server at `http://localhost:3000/`. The active-route navigation renders as a compact segmented control with Overview, Calculator, Imaging, Records, and Methods. The hero retains the light kidney-to-tablet video/poster composition, readable graphite copy, and visible primary/secondary actions. The hero metrics settle to the deterministic values shown in the evidence cards: eGFR 43.6, 2-year KFRE 0.9%, and KDIGO G3b A2.

The lower evidence cards render at a readable contrast after the entrance reveal settles. The new Three.js signal field appears in the computation section as a subdued teal/silver interactive canvas with the caption “Three inputs, one traceable assessment.” It is decorative and the four computation steps remain normal HTML below it. The scene uses the existing R3F implementation with a reduced particle budget and light clinical colors.

The browser view showed no visible layout collision or runtime error during the desktop pass. The first screenshot was captured while animations were still settling and showed animated metric values near zero; the settled browser view confirmed the final values. This is expected behavior from the existing anime.js count-up enhancement, not a data mismatch.
